name: Build & Test

on:
  push:
    branches:
    - main
  pull_request:
    branches:
    - main
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Build
      run: dotnet build src --configuration Release
    - name: Component Tests
      run: dotnet test test/ComponentTests --configuration Release /p:CollectCoverage=true /p:CoverletOutputFormat=opencover
    - name: Unit Tests
      run: dotnet test test/UnitTests --configuration Release /p:CollectCoverage=true /p:CoverletOutputFormat=opencover
    - name: Upload net5 Code Coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        fail_ci_if_error: true
        files: /home/runner/work/pulse-prometheus/pulse-prometheus/test/ComponentTests/coverage.net5.0.opencover.xml,/home/runner/work/pulse-prometheus/pulse-prometheus/test/UnitTests/coverage.net5.0.opencover.xml
        name: net5-code-coverage
        token: ${{ secrets.CODECOV_TOKEN }}
        verbose: true
    - name: Upload net6 Code Coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        fail_ci_if_error: true
        files: /home/runner/work/pulse-prometheus/pulse-prometheus/test/ComponentTests/coverage.net6.0.opencover.xml,/home/runner/work/pulse-prometheus/pulse-prometheus/test/UnitTests/coverage.net6.0.opencover.xml
        name: net6-code-coverage
        token: ${{ secrets.CODECOV_TOKEN }}
        verbose: true